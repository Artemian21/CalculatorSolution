name: CI Pipeline

on:
  pull_request:
    branches: [develop]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore CalculatorSolution.sln

      - name: Build solution
        run: dotnet build CalculatorSolution.sln --configuration Release

      - name: Run tests
        run: dotnet test CalculatorSolution.sln --logger "trx;LogFileName=test_results.trx"

  analysis:
    runs-on: windows-latest
    needs: build
    steps:
      - uses: actions/checkout@v3

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Count lines of code with CLOC
        run: |
          choco install cloc -y
          cloc Calculator > cloc-report.txt
          type cloc-report.txt

      - name: Run SonarCloud analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_ORG: ${{ secrets.SONAR_ORG }}
        run: |
          dotnet tool install --global dotnet-sonarscanner
          dotnet sonarscanner begin /k:"Artemian21_CalculatorSolution" /o:"$env:SONAR_ORG" /d:sonar.login="$env:SONAR_TOKEN"
          dotnet build CalculatorSolution.sln
          dotnet sonarscanner end /d:sonar.login="$env:SONAR_TOKEN"

      - name: Code Review Check for develop
        uses: actions/github-script@v6
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const pr = context.payload.pull_request;
            if (!pr) {
              core.setFailed("Це не Pull Request");
              return;
            }

            const prNumber = pr.number;
            const prAuthor = pr.user.login;

            const { data: reviews } = await github.rest.pulls.listReviews({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: prNumber
            });

            console.log("Знайдені рев’ю:");
            reviews.forEach(r => {
              console.log(`User: ${r.user.login}, State: ${r.state}`);
            });

            if (pr.user.login === context.actor) {
              console.log("Автор PR - це виконавець workflow, рев’ю пропущено");
            } else {
              const approved = reviews.some(review => review.state === 'APPROVED');
              if (!approved) {
                core.setFailed("Код-рев’ю не схвалено");
              } else {
                console.log("Є схвалене рев’ю");
              }
            }

      - name: Setup reviewdog
        uses: reviewdog/action-setup@v1.4.0
        with:
          reviewdog_version: latest

      - name: Run StyleCop with Reviewdog
        uses: reviewdog/action-stylecop@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          reporter: github-pr-review
          level: error

      - name: Run dotnet format with Reviewdog
        run: |
            dotnet format --check | reviewdog -f=msbuild -name="dotnet-format" -reporter=github-pr-review -level=info